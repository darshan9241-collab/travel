# Copy this file to .env.local and fill in your own values.
# .env.local is gitignored — never commit real secrets.

# Powers the contact form and newsletter signup (src/app/api/contact,
# src/app/api/newsletter). Get a free key at https://resend.com/api-keys
RESEND_API_KEY=
